{
  "version": 2,
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "installCommand": "pip install -r requirements.txt && python -m spacy download en_core_web_sm && python -c \"import nltk; nltk.download('punkt'); nltk.download('stopwords')\""
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/app.py"
    }
  ]
}
